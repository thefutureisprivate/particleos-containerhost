rebuild-container-host:
  steps:
    - trigger_services:
        project: home:thefutureisprivate
        package: particleos-containerhost
  filters:
    event: push
    branches:
      only:
        - main
