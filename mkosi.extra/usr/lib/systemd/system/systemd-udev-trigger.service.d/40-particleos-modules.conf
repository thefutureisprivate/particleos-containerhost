[Unit]
# Load the complete, image-owned module set before udev coldplug can request
# overlapping networking modules from another task. This also makes the
# irreversible module-lockdown boundary deterministic.
Requires=systemd-modules-load.service
After=systemd-modules-load.service
